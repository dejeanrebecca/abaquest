rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Extended Test Mode: Allows open access until May 21, 2026
      allow read, write: if request.time < timestamp.date(2026, 5, 21);
    }
  }
}
